const assert = require('assert');
const mdMetadataParser = require('../index.js');

describe('mdMetadataParser', () => {
  it('has a test', () => {
    assert(false, 'mdMetadataParser should have a test');
  });
});
